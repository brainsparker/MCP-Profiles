{
  "$comment": "Example MCP client config (e.g. Claude Desktop's claude_desktop_config.json). Point the gateway at your profiles dir and downstream registry. Switch profiles at runtime by calling the switch_profile tool.",
  "mcpServers": {
    "mcp-profiles": {
      "command": "node",
      "args": [
        "/absolute/path/to/mcp-profiles/dist/index.js",
        "--profiles-dir",
        "/absolute/path/to/mcp-profiles/profiles",
        "--config",
        "/absolute/path/to/mcp-profiles/mcp-profiles.config.json",
        "--profile",
        "growth-pm"
      ],
      "env": {
        "AMPLITUDE_KEY": "...",
        "NOTION_TOKEN": "...",
        "LINEAR_TOKEN": "..."
      }
    }
  }
}
